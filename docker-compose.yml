version: '3.8'
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: flashmind
      POSTGRES_PASSWORD: flashmind
      POSTGRES_DB: flashmind
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: